# Plan Document Template

`PLAN.v{N}.md` 的标准结构。每个 `{占位符}` 需替换为实际内容。

---

```markdown
# PLAN.v{N}.md — {项目名称} V{N} 实施计划

> **创建日期**: {YYYY-MM-DD}
> **状态**: 待执行
> **策略**: {从零重写 | 分阶段对齐 | 按 proposal 逐条修}
> **参考文档**: [V{N} PRD](./docs/prd/) / [V{N} SPEC](./docs/specs/)

---

## 总体策略

{一段话描述策略选择理由，包含关键的量化数据。}

**代码量估算**：

| 操作 | 行数 |
|------|------|
| 删除 | ~{X} |
| 扩展修改 | ~{X} |
| 全新编写 | ~{X} |
| 测试新增/更新 | ~{X} |
| **净变化** | **~{X}** |

---

## 阶段 {N}：{阶段标题}

> **目标**: {一句话目标}
> **预计工作量**: {小 | 中 | 大}
> **对应 SPEC**: [{spec_file}](./docs/specs/{spec_file})
> **前置依赖**: {阶段 X | 无}

### {N}.1 {子任务组标题}

| 任务 | 文件 | 说明 |
|------|------|------|
| {具体操作} | `{文件路径}` | {完成标准} |

{如有复杂逻辑，用文字段落或表格补充关键方法/接口/字段变化}

### {N}.{M} 验收标准

- [ ] {具体的、可测试的验收条件}
- [ ] `npm run build` 编译通过
- [ ] `npm test` 全部绿色

---

{重复阶段块...}

---

## 阶段间依赖总览

{mermaid flowchart 展示阶段依赖关系}

---

## 文件变更清单

### 新增文件

| 文件 | 阶段 | 说明 |
|------|------|------|

### 删除文件

| 文件 | 阶段 | 说明 |
|------|------|------|

### 修改文件

| 文件 | 阶段 | 变化要点 |
|------|------|---------|

---

## 质量保障

### 测试策略

| 测试类型 | 覆盖范围 | 文件 |
|----------|---------|------|

### 每阶段验收

每阶段完成后：
1. `npm run build` 编译通过
2. `npm test` 全部绿色
3. 无编译警告
4. 新增代码有对应测试
5. 代码符合 SPEC 对应章节

---

## 风险与缓解

| 风险 | 影响 | 缓解措施 |
|------|------|---------|
```
