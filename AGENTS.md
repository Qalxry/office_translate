# AGENTS.md

下面是您的编程规范和指南，旨在指导您更高效地与用户交互并完成任务。

本文件中的 `.agents/skills/<name>` 均指目标项目内平铺安装的 skill。只有对应目录存在时，才应用该 skill 的专属工作流和附加规则；集合未安装某个 skill 时，不应把缺失路径视为项目错误，也不应未经用户要求自动扩大集合。

---

## Tools 调用规范

### 提问工具

如果您是主代理（直接与用户交互的代理）：
- **你的最终回答应当先输出长段内容后再在末尾调用 `提问工具` 进行提问**，这样可让用户充分理解上下文并做出更准确的回答。
- 用户提出**较为模糊或者不合理**的要求时，**应当先通过 `提问工具` 与用户进行商议**，而不是直接执行，以避免理解偏差。
- 在执行完成后，**先向用户汇报结果并征求意见**，而不是直接开始下一步，以避免偏差，并且让用户有机会进行干预和调整。

如果您是子代理（被主代理调用的代理）：
- **最终必须以纯文本消息结束，不调用 `提问工具`**，以便将结果直接返回给主代理进行后续处理。
- 禁止调用 `提问工具` 进行提问，因为它会导致主代理无法正确获取结果。

#### 注1

- 在 VSCode Copilot 环境中，该工具名称为 `vscode_askQuestions`，一般无问题数量限制。
- 在 Codex 环境中，该工具名称为 `request_user_input` ，且只能在 PLAN 模式下使用。
- 在 Claude Code 环境中，该工具名称为 `AskUserQuestions`，但可能有问题数量限制。

#### 注2

如果您是主代理，而您所处的环境不支持 `提问工具`，则可以使用以下格式替代：

```markdown
> **Q{No}.{问题短标题}**
> {详细的问题内容，包含必要的背景信息、分析等，以便用户理解并做出准确回答。}
> **A{1}. {选项标题}**: {详细回答内容}（ {陈述利弊和推荐等级⭐|⭐⭐|⭐⭐⭐|⭐⭐⭐⭐|⭐⭐⭐⭐⭐}）
> **A{2}. {选项标题}**: {详细回答内容}（ {同上}）
> **A{No}. {选项标题}**: {详细回答内容}（ {同上}）
> **A{No}**: 请输入（总是保留该选项，允许用户输入自由文本回答）
> 
> {一些补充说明和建议，以便用户更好地理解问题和选项，做出准确回答。}

> **Q2. XXX**
```

### 子代理工具

- 对于完整输出原文的需求，请勿使用子代理，直接自行通过 `文件读取` / `终端` 等工具读取文件内容即可，因为子代理通常会删减内容，无法满足您的需求。
- 如果您是主代理，在调用子代理时需要**明确告知它是一个子代理，并且最终必须以纯文本消息结束，不调用 `提问工具`**。

### 终端工具

**推荐使用同步模式**，这样完成后一次性获取结果，从而节省大量的对话轮次和费用。
**除非确实需要后台持续运行，否则不推荐使用异步模式**，因为这可能导致频繁汇报终端输出，产生大量不必要的对话轮次和费用。

---

## 提案与文档更新规范

当 `create-proposals` 集合已安装时，提案与文档更新的完整规范（分类标准、写入原则、审查/创意提示、编号规则、去重、模板示例等）定义在 [.agents/skills/create-proposals/rules.md](.agents/skills/create-proposals/rules.md)，该文件是权威来源。创建或更新提案时先读它。未安装时以用户当前要求和本节速查为准。`AGENTS.md` 此处仅保留极简速查，避免与 rules.md 重复维护。

文件分工速查：

| 文件 | 用途 |
|---|---|
| `proposals.p0.md` | 关键缺陷（数据丢失、安全绕过、核心流程崩溃等） |
| `proposals.p1.md` | 重要实现缺口、可靠性失败、集成断裂、规格明显漂移 |
| `proposals.p2.md` | 低风险 bug、可维护性、诊断缺口、边缘情况 |
| `proposals.feat.md` | 新功能能力、流程改进、自动化等计划性工作 |
| `docs.updates.md` | 仅文档同步：实现更丰富、可接受漂移、文档过时/缺失。代码弱于文档要求时改写 `proposals.p0/p1/p2.md` |

通用要点（详见 rules.md）：每条提案**标题前加序号**、**文件位置含行号范围**、完成后标记 `✅` 并写最终实现情况；尽量**少而精**，仅在严重程度/目标文件/职责/实现路径不同时才拆分；写前**去重**，近重复则更新而非新建。

## 项目记忆策略 `.agents/memory/`

项目把可复用的经验沉淀在 `.agents/memory/`（可能未创建），索引文件是 `.agents/memory/MEMORY.md`，每条经验是 `*.md` 单事实单文件（带 frontmatter，`metadata.type` 取 `pitfall`/`trajectory`/`dead-end`/`preference`）。这套记忆是项目级、随仓库版本控制、跨环境一致（Claude Code / Codex 等都能读）。

**重要**：与 Claude Code 的用户级记忆系统（`~/.claude/projects/<hash>/memory/`，harness 会自动加载其 `MEMORY.md`）不同，项目级 `.agents/memory/MEMORY.md` **不会被 harness 自动加载**。因此代理必须主动读取，规则如下：

- **任务开始时**：先读 `.agents/memory/MEMORY.md` 索引，判断是否有与当前任务相关的经验条目；若有，按索引指向的文件读取相关条目后再动手，避免重复踩坑或重复探索。
- **遇到 pitfall / trajectory 问题时**：若当前任务卡在某个坑或某条路径上，先查 `.agents/memory/` 是否已有对应记录，再决定是否重新探索。
- **经验沉淀时**：当对话中出现非显然的经验（踩坑细节、经探索才找到的最优轨迹、走不通的死路、浮现的偏好），若已安装 `distill-experience`，使用该技能（`.agents/skills/distill-experience/`）将其蒸馏写入 `.agents/memory/`。该技能负责草拟→对抗式自审过滤→呈用户确认→只写确认通过的，并更新索引。读取应用经验由本策略负责，写入由该技能负责，二者不要混淆。

经验只在「真非显然、已验证、可复用、仓库未记录、不误导后续 agent」时才值得沉淀；稀松平常或显而易见的知识不应记录，避免噪音。判定标准与写入流程详见 `.agents/skills/distill-experience/SKILL.md` 和 `.agents/skills/distill-experience/rules.md`。

## 外部参考代码库 `ref_repos/`

本地克隆的第三方参考仓库集合（已加入 `.gitignore`，不参与版本控制），用于在设计和实现时查阅参考实现、借鉴模式和架构。

管理方式：若已安装 `index-ref-repos`，由该技能（`.agents/skills/index-ref-repos/`）维护一套分层索引文件：

| 文件 | 用途 |
|---|---|
| `ref_repos/INDEX.md` | 根索引，含仓库全景分析、探索策略、仓库状态总表、主题索引链接 |
| `ref_repos/INDEX.REPO.{slug}.md` | 单仓库深度索引：速查卡片 → 设计哲学与核心决策 → 架构地图 → 核心工作流 → 关键抽象与设计模式 → 分级阅读指南 → 对当前项目的价值评估 |
| `ref_repos/INDEX.TOPIC.{slug}.md` | 跨仓库主题对比索引：主题定位、对比矩阵、逐仓库深析、推荐阅读顺序、综合分析 |

索引设计遵循**分层可读**（30 秒速查 + 架构级深度）、**证据驱动**（所有模式/结论附带具体代码路径证据）、**决策导向**（解释「为什么这么做」而非仅描述「做了什么」）。

代理在使用 `ref_repos/` 时应先读 `ref_repos/INDEX.md` 确定相关仓库和阅读路径，再按需深入对应的 `INDEX.REPO.*.md` 或 `INDEX.TOPIC.*.md`。当添加新参考仓库时，使用 `index-ref-repos` 技能进行增量索引。
