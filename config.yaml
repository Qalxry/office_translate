# office_translate 全局配置
# 相对路径以本文件所在目录为基准；缺省项用内置默认值。

# 工作区根目录，每个翻译任务一个子文件夹
work_dir: work

# 每个任务内 apply 产物的存放子目录名
output_dir: output

# 对照版「原文+分隔符+译文」的分隔符。
# 写字面转义即可，\\n \\r \\t 会在加载时还原为真实字符。
sep: '\n'
